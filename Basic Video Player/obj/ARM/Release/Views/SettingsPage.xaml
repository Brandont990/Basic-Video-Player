<Page x:ConnectionId='1'
    x:Class="Basic_Video_Player.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helper="using:Basic_Video_Player.Helpers"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:xaml="using:Windows.UI.Xaml"
    Style="{StaticResource PageStyle}"
    mc:Ignorable="d">

    <Page.Resources>
        <helper:EnumToBooleanConverter x:Key="EnumToBooleanConverter" EnumType="ElementTheme" />
    </Page.Resources>

    <Grid Margin="{StaticResource MediumLeftRightMargin}">

        <Grid.RowDefinitions>
            <RowDefinition Height="55" />
            <RowDefinition Height="*" />
            <RowDefinition Height="95" />
        </Grid.RowDefinitions>

        <TextBlock
            x:Uid="Settings_Title"
            Grid.Row="0"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Style="{StaticResource PageTitleStyle}" />

        <CommandBar Background="Transparent">
            <AppBarButton x:ConnectionId='8'
                HorizontalAlignment="Right"
                                         
                Icon="Play"
                Label="Player" />
        </CommandBar>

        <StackPanel x:ConnectionId='2' x:Name="ContentArea" Grid.Row="1">
            <TextBlock x:Uid="Settings_Personalization" Style="{ThemeResource SubtitleTextBlockStyle}" />
            <StackPanel Margin="{StaticResource SmallTopBottomMargin}">
                <TextBlock x:Uid="Settings_Theme" />

                <StackPanel Margin="{StaticResource XSmallTopMargin}">
                    <RadioButton x:ConnectionId='5'
                        x:Uid="Settings_Theme_Light"
                                                           
                        FontSize="15"
                        GroupName="AppTheme"
                                                                                                                                                   >
                        <RadioButton.CommandParameter>
                            <xaml:ElementTheme>Light</xaml:ElementTheme>
                        </RadioButton.CommandParameter>
                    </RadioButton>
                    <RadioButton x:ConnectionId='6'
                        x:Uid="Settings_Theme_Dark"
                                                           
                        FontSize="15"
                        GroupName="AppTheme"
                                                                                                                                                  >
                        <RadioButton.CommandParameter>
                            <xaml:ElementTheme>Dark</xaml:ElementTheme>
                        </RadioButton.CommandParameter>
                    </RadioButton>
                    <RadioButton x:ConnectionId='7'
                        x:Uid="Settings_Theme_Default"
                                                           
                        FontSize="15"
                        GroupName="AppTheme"
                                                                                                                                                     >
                        <RadioButton.CommandParameter>
                            <xaml:ElementTheme>Default</xaml:ElementTheme>
                        </RadioButton.CommandParameter>
                    </RadioButton>
                </StackPanel>
            </StackPanel>

            <TextBlock x:Uid="Settings_About" Style="{ThemeResource SubtitleTextBlockStyle}" />

            <StackPanel Margin="{StaticResource XSmallTopMargin}">
                <TextBlock x:ConnectionId='3' Style="{ThemeResource BodyTextBlockStyle}"                                                 />
                <TextBlock
                    x:Uid="Settings_AboutDescription"
                    Margin="{StaticResource XSmallTopMargin}"
                    Style="{ThemeResource BodyTextBlockStyle}" />

                <HyperlinkButton x:Uid="Settings_PrivacyTermsLink" Margin="{StaticResource XSmallTopMargin}" />

                <HyperlinkButton x:Uid="Settings_TermsLink" Margin="{StaticResource XSmallTopMargin}" />

                <HyperlinkButton x:ConnectionId='4'
                    x:Name="FeedbackLink"
                    x:Uid="FeedbackHub_Link"
                    Margin="{StaticResource XSmallTopMargin}"
                                              
                    Visibility="Visible" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>

